# Lambda Function in Python: Addition of Two Numbers

## 🎯 Aim
To write a Python program that defines a **lambda function** which takes two arguments `a` and `b`, and returns their sum.

## 🧠 Algorithm
1. Get two integer inputs from the user.
2. Use a **lambda function** to define a function `f` that returns `a + b`.
3. Call the function with the user inputs and print the result.

## 🧾 Program
~~~
# Lambda function for addition

# Input from user
a = int(input("Enter first number: "))
b = int(input("Enter second number: "))

# Lambda function
f = lambda a, b: a + b

# Function call and print result
print("Sum:", f(a, b))
~~~
## Output

<img width="489" height="249" alt="Screenshot 2026-03-24 135707" src="https://github.com/user-attachments/assets/bdef1368-eec7-449b-b7ad-11d8265ae9b4" />

## Result
Thus, the Python program using a lambda function to add two numbers was successfully executed and verified.
